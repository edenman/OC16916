import UIKit

class ViewController: UIViewController {
  override func viewDidLoad() {
    super.viewDidLoad()
//    NSLayoutConstraint.autoCreateAndInstallConstraints does not autocomplete in AppCode, but does in Xcode.
  }
}
